class AlgorithmAnalysis {
  static   operation findElement( elements : Sequence(int), size : int, target : int) : int
  pre: true
  post: true
  activity: (     var index : int ;
         index := 0 ; while index < size do ( (     if (elements[index+1] = target) then (     return index ) else skip )  ;     index := index + 1 ) ;
     return -1 );
  static   operation main( args : Sequence(String)) : void
  pre: true
  post: true
  activity: (     var numbers : Sequence(int) ; numbers := Sequence{10,20,30,40,50} ;
     var target : int ; target := 30 ;
     var result : int ; result := findElement(numbers, numbers->size(), target) ;
     if (result /= -1) then (     OclFile["System.out"].println("Element found at index: " + result) ) else (     OclFile["System.out"].println("Element not found.") ) );
}
 <EOF>