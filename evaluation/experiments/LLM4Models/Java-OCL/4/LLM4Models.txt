class SubsequenceDivisibleByN {
  static   operation countDivisibleSubsequences( inputString : String, divisor : int) : int
  pre: true
  post: true
  activity: (     var length : int ; length := inputString->size() ;
     var prevModulo : int ; prevModulo := 0 ;
    var currModulo : int ; currModulo := 0 ;
    if ((inputString->at(0+1) - ('0')->char2byte()) mod divisor = 0) then     prevModulo := prevModulo + 1 ; else skip ;
        var i : int ; i := 1 ; while i < length do ( (     currModulo := (inputString->at(i+1) - ('0')->char2byte()) mod divisor ;
     prevModulo := prevModulo+((prevModulo + (if currModulo = 0 then 1 else 0 endif))) ;
     var temp : int ; temp := (currModulo * 10 + (inputString->at(i+1) - ('0')->char2byte())) mod divisor ;
     prevModulo := prevModulo+(if temp = 0 then 1 else 0 endif) )  ;     i := i + 1 ) ;
    return prevModulo
 );
  static   operation main( args : Sequence(String)) : void
  pre: true
  post: true
  activity: (     var numberString : String ; numberString := "12345" ;
     var divisor : int ; divisor := 3 ;
     OclFile["System.out"].println("Divisible subsequences: " + countDivisibleSubsequences(numberString, divisor)) );
}
 <EOF>