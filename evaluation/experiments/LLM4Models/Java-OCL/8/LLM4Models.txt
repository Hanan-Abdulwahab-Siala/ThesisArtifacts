class ConeVolumeCalculator {
  static   operation calculateVolume( surfaceArea : double) : double
  pre: true
  post: true
  activity: (     if (surfaceArea < 0) then     return -1 else skip ;
     var radius : double ; radius := (surfaceArea * (2)->sqrt())->oclAsType(double) / 3 ;
    var height : double ; height := (2 * surfaceArea) / 3 ;
    var volume : double ; volume := (3.14 * radius->pow(2) * height)->oclAsType(double) ;
    return volume
 );
  static   operation main( args : Sequence(String)) : void
  pre: true
  post: true
  activity: (     var surfaceArea : double ; surfaceArea := 5 ;
     OclFile["System.out"].println(calculateVolume(surfaceArea)) );
}
 <EOF>