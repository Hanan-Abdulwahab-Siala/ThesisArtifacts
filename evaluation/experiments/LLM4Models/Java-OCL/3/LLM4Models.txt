class EquilibriumIndexFinder {
  static   operation findEquilibriumIndex( array : Sequence(int), length : int) : int
  pre: true
  post: true
  activity: (     var totalSum : int ; totalSum := 0 ;
     var leftSum : int ; leftSum := 0 ;
        var i : int ; i := 0 ; while i < length do (     totalSum := totalSum+(array[i+1]) ;  ;     i := i + 1 ) ;
        var i : int ; i := 0 ; while i < length do ( (     totalSum := totalSum-(array[i+1]) ;
     if (leftSum = totalSum) then (     return i ) else skip ;
     leftSum := leftSum+(array[i+1]) )  ;     i := i + 1 ) ;
    return -1
 );
  static   operation main( args : Sequence(String)) : void
  pre: true
  post: true
  activity: (     var array : Sequence(int) ; array := Sequence{1,3,5,2,2} ;
     var result : int ; result := findEquilibriumIndex(array, array->size()) ;
     if (result /= -1) then     OclFile["System.out"].println("Equilibrium index found at: " + result) ; else     OclFile["System.out"].println("No equilibrium index found.") ; );
}
 <EOF>