class Main {
  static   operation main( args : Sequence(String)) : void
  pre: true
  post: true
  activity: (     var activation : double ; activation := MathLib.random() ;
     OclFile["System.out"].println("Forward pass: " + activation) ;
    var checkpointingEnabled : boolean ; checkpointingEnabled := false ;
    var checkpoint : double ; checkpoint := 0.0 ;
    if (checkpointingEnabled) then (     checkpoint := activation ;
     activation := checkpoint - 0.2 ) else (     activation := activation-(0.2) ) ;
    OclFile["System.out"].println("Updated activation (without checkpointing): " + activation) ;
 );
}
 <EOF>