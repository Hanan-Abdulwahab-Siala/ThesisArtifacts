class Person {
  static operation newPerson() : Person
  pre: true
  post: Person->exists( _x | result = _x );
  attribute full_name : OclAny := full_name;
  attribute student_id : OclAny := student_id;
  attribute age : OclAny := age;
  attribute score : OclAny := score;
  operation initialise(full_name : OclAny,student_id : OclAny,age : OclAny,score : OclAny) : Person
  pre: true post: true
  activity:
    self.full_name := full_name ;
    self.student_id := student_id ;
    self.age := age ;
    self.score := score;
    return self;
  operation increase_score(additional_score : OclAny)
  pre: true post: true
  activity:
    self.score := self.score + additional_score;
  operation celebrate_birthday() : OclAny
  pre: true post: true
  activity:
    self.age := self.age + 1 ;
    return self.age;
}
 class FromPython {
  operation initialise()
  pre: true post: true
  activity:     skip ;
    var student : Person := (Person.newPerson()).initialise("Jorge", "123", 18, 80) ;
    var new_age : OclAny := student.celebrate_birthday() ;
    execute (new_age)->display();
}