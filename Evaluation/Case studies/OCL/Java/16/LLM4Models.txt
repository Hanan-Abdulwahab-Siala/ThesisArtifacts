class Mapper<T,V> {
   operation add( array : T, item : V) : void
  pre: true
  post: true
  activity: (     array->excludes(item) ;
     var mapper : Mapper<CustomList,int> ; mapper := Mapper<CustomList,int>.newMapper() ;
    var mapper : Mapper<CustomList,OclAny> ;
    mapper := Mapper<CustomList,Boolean>.newMapper() ;
    mapper := Mapper<CustomList,int>.newMapper() ;
 );
}
 <EOF>