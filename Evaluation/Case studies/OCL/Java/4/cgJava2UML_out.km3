class HelloWorld {
  static   operation main( args : Sequence(String)) : void
  pre: true
  post: true
  activity: (         var i : int ; i := 0 ;
    var j : int ; j := 9 ; while i < 10 do ( (     execute doSomething() )  ;     i := i + 1 ;     j := j-(3) ) ; 
       while true do ( (     execute doSomething() )  ) ; 
    for (i : intArray) do ( (     execute doSomething(i) )  ) ; 
    execute someMethod() ; 
        var i : int ; i := 0 ; while i < 10 do ( (     while (true) do (     break ) )  ;     i := i + 1 ) ; 
        var i : int ; i := 0 ; while i < 10 do ( (     while (true) do (     break ) )  ;     i := i + 1 ) ; 
    var ch : int ;
        ch := getChar() ;
    while (ch) do (    ch := getChar() ; (     if (ch = ' ') then (     continue ) else skip ; 
     execute doSomething() ) ;     ch := getChar() ;
 ) ;  
    for (str : stringsArr) do ( (     var strChars : Sequence(String) ; strChars := str->characters() ;
     for (ch : strChars) do ( (     if (ch = ' ') then (     continue ) else skip ; 
     execute doSomething(ch) )  ) )  ) ; 
    if (streamClosed) then (     return ) else skip ; 
    execute readFromStream() ; 
    var result : int ; result := a + b ;
    return result 
 );

}

 <EOF>
